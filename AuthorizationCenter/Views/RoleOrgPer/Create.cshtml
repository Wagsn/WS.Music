@model AuthorizationCenter.Entitys.RoleOrgPer

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>RoleOrgPer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">Role Name</label>
                <select asp-for="RoleId" class ="form-control" asp-items="ViewBag.RoleId"></select>
            </div>
            <div class="form-group">
                <label class="control-label">Orgnazition Name</label>
                <select asp-for="OrgId" class ="form-control" asp-items="ViewBag.OrgId"></select>
            </div>
            <div class="form-group">
                <label class="control-label">Permission Name</label>
                <select asp-for="PerId" class ="form-control" asp-items="ViewBag.PerId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
