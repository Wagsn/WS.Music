using System;
using System.Collections.Generic;
using System.Text;

namespace WS.Core
{
    public static class StringExtension
    {
        /// <summary>
        /// 占位符替换，默认 {{KEY}}，自定义 ${KEY}
        /// </summary>
        /// <param name="pairs"></param>
        /// <returns></returns>
        public static string Replace(List<KeyValue> pairs)
        {

            return "";
        }
    }
}
